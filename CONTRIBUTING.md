# Contributing Guide

이 문서는 YH Lab Organization 내 레포에 기여할 때 공통으로 따르는 절차입니다.
개별 레포에 별도의 `CONTRIBUTING.md`가 있다면 그 내용이 우선하며, 이 문서는 그런 문서가 없는 레포에 기본으로 적용됩니다.

---

## 1. 작업 시작 전

- 이미 진행 중인 작업과 겹치지 않는지 확인해주세요. (레포 Issues 탭 또는 랩 내 커뮤니케이션 채널에서 확인)
- 새로운 기능/실험을 시작할 때는 가능하면 Issue를 먼저 만들어 목적을 간단히 남겨주세요. (필수는 아니지만, 나중에 "왜 이 코드가 있는지" 추적할 때 큰 도움이 됩니다.)

## 2. 브랜치 생성

```bash
git checkout main
git pull origin main
git checkout -b feature/작업내용
```

- `main`에서 최신 상태로 새 브랜치를 만듭니다.
- 브랜치 이름은 `feature/`, `fix/`, `exp/`, `docs/` 등 접두사로 목적을 표시합니다.

## 3. 작업 및 커밋

- 커밋은 작은 단위로 자주 나눠주세요. (하나의 거대한 커밋보다 리뷰와 이력 추적이 쉽습니다.)
- 커밋 메시지는 `feat: vdM clash 검사 로직 추가` 처럼 prefix + 간단한 설명 형태를 권장합니다.
- 대용량 파일(모델 가중치, raw 데이터, 결과 PDB 다수 등)은 커밋하지 말고 `.gitignore`에 추가하거나, 필요하다면 별도 저장 방식을 랩 내에서 상의해주세요.

## 4. Pull Request 올리기

- `main`으로 향하는 PR을 생성합니다.
- PR 제목: 작업 내용을 한 줄로 요약
- PR 설명에 포함할 내용:
  - **무엇을** 변경했는지
  - **왜** 변경했는지 (배경/목적)
  - 어떻게 테스트/검증했는지 (예: "b40 타겟으로 실행해서 기존 대비 clash-free ensemble 수 확인함")
  - 관련된 Issue가 있다면 링크

## 5. 리뷰

- 가능하면 리뷰어 1명을 지정해주세요. 랩 인원 상황상 즉시 리뷰가 어려울 수 있으니, 급한 작업이면 미리 구두로 언질을 주는 것도 좋습니다.
- 리뷰어는 다음을 확인해주세요.
  - 코드가 README/문서와 일치하게 동작하는지
  - 의존성 변경이 있다면 `environment.yml`/`requirements.txt`에 반영되었는지
  - 불필요한 대용량 파일이나 개인 경로(`/home/username/...` 같은 하드코딩 경로)가 포함되지 않았는지

## 6. Merge

- 리뷰 승인 후 본인이 직접 merge합니다. (Squash merge를 권장하지만 필수는 아닙니다.)
- Merge 후에는 작업 브랜치를 삭제해주세요.

```bash
git branch -d feature/작업내용
git push origin --delete feature/작업내용
```

---

## 참고: 강제되지 않는 규칙입니다

현재 Organization은 GitHub Free 플랜이라 Private 레포에서는 이 규칙들이 시스템적으로 강제되지 않습니다.
즉 `main`에 직접 push하는 것도 기술적으로는 가능합니다. 하지만 랩 전체의 코드 안정성과 재현성을 위해 위 절차를 지켜주시길 부탁드립니다.
